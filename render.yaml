services:
  - type: web
    name: anipix-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn anipix_bot:app --timeout 120
    envVars:
      - key: WEBHOOK_URL
        sync: false
      - key: PORT
        value: 10000
